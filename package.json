{
  "name": "spot-the-code",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ],
    "catalog": {
      "dotenv": "^17.2.2",
      "zod": "^4.1.13",
      "typescript": "^5",
      "@types/node": "^22.13.14"
    }
  },
  "type": "module",
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "check-types": "bun run --filter '*' check-types",
    "dev:api": "bun run --filter @spot-the-code/api dev",
    "dev:native": "bun run --filter native dev",
    "dev:web": "bun run --filter web dev"
  },
  "packageManager": "bun@1.3.3",
  "dependencies": {
    "@spot-the-code/env": "workspace:*",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/node": "catalog:",
    "@spot-the-code/config": "workspace:*"
  }
}
